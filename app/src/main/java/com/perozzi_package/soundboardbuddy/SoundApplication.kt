
package com.perozzi_package.soundboardbuddy
/*
import android.app.Application
import com.example.soundboardbuddy.UI.soundboard.SoundViewModelFactory
import com.example.soundboardbuddy.data.db.SoundDatabase
import com.example.soundboardbuddy.data.repositories.SoundRepository
import org.kodein.di.Kodein
import org.kodein.di.KodeinAware
import org.kodein.di.android.x.androidXModule
import org.kodein.di.generic.bind
import org.kodein.di.generic.instance
import org.kodein.di.generic.provider
import org.kodein.di.generic.singleton

class SoundApplication: Application(), KodeinAware {

    override val kodein: Kodein = Kodein.lazy {
        import(androidXModule(this@SoundApplication))
        bind() from singleton { SoundDatabase(instance()) }
        bind() from singleton { SoundRepository(instance()) }
        bind() from provider { SoundViewModelFactory(instance()) }
    }

}*/
