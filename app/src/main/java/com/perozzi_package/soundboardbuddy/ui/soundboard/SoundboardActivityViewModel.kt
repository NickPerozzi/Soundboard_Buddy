package com.perozzi_package.soundboardbuddy.ui.soundboard

import android.content.Context.AUDIO_SERVICE
import android.media.AudioManager
import androidx.core.content.ContextCompat.getSystemService
import androidx.lifecycle.ViewModel

class SoundboardActivityViewModel() : ViewModel() {

    // TODO(Pass in context from SoundboardActivity to use getSystemServices)
    /*private var audioManager = getSystemService(this, AUDIO_SERVICE) as AudioManager

    var maxVolume = audioManager.getStreamMaxVolume(AudioManager.STREAM_MUSIC)
    var currentVolume = audioManager.getStreamVolume(AudioManager.STREAM_MUSIC)*/

}